[package]
name = "direnv-instant"
version = "1.2.0"
edition = "2024"

[dependencies]
nix = { version = "0.31", features = ["process", "signal", "term", "fs", "poll", "socket", "uio"] }
vt100-split = { path = "crates/vt100-split" }
